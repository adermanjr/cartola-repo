<%= form_for(cartoleiro) do |f| %>
  <% if cartoleiro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cartoleiro.errors.count, "error") %> prohibited this cartoleiro from being saved:</h2>

      <ul>
      <% cartoleiro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time %>
    <%= f.text_field :time %>
  </div>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :camisa %>
    <%= f.check_box :camisa %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
