<%= form_with(model: cartoleiro) do |form| %>
  <% if cartoleiro.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cartoleiro.errors.count, "error") %> prohibited this cartoleiro from being saved:</h2>

      <ul>
        <% cartoleiro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :time, style: "display: block" %>
    <%= form.text_field :time %>
  </div>

  <div class="field">
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :camisa, style: "display: block" %>
    <%= form.check_box :camisa %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
